<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <title>PDF File generated by Globe Technologie</title>

  <style>
    @import url('https://fonts.googleapis.com/css2?family=Source+Sans+Pro:ital,wght@0,400;0,700;1,400;1,700&display=swap');

    @page {
      size: auto;
      margin: 0.75cm !important;
    }

    @media print {

      .widget,
      img {
        display: block;

      }

      .widget-title {
        font-weight: bold;
        margin: 10px 0;
        page-break-after: avoid;
        break-after: avoid;
      }

      .widget img {
        width: 100%;
      }

      .page {
        page-break-after: auto;
        padding-bottom: 30px;
      }

      a::after {
        content: "TEST";
      }
    }

    .clearfix:after {
      content: "";
      display: table;
      clear: both;
    }

    .clearfix {
      margin: .5cm 0;
    }

    a {
      color: rgb(0, 135, 195);
      text-decoration: none;
    }

    body {
      background: rgb(255, 255, 255);
      font-family: 'Source Sans Pro', sans-serif;
      font-size: 14px;
    }

    header {
      padding: 10px 0;
      margin-bottom: 20px;
      border-bottom: 1px solid rgb(170, 170, 170);
    }

    #logo {
      float: left;
      margin-top: 8px;
    }

    #logo img {
      height: 50px;
    }

    #company {
      float: right;
      text-align: right;
    }

    #client .to {
      color: rgb(119, 119, 119);
    }

    h2.name {
      font-size: 1.4em;
      font-weight: bold;
      margin: 0;
    }


    footer {
      color: rgb(119, 119, 119);
      width: 100%;
      height: 30px;
      position: fixed;
      bottom: 0;
      border-top: 1px solid rgb(170, 170, 170);
      padding: 8px 0;
      text-align: center;
      font-size: small;
    }

    .flex-container {
      display: flex;
      flex-wrap: wrap;
    }

    .flex-container>div {
      margin: 10px;
      text-align: center;
    }
  </style>
</head>

<body>
  <header class="clearfix">
    <div id="logo">
      <img src="{{logo}}">
    </div>
    <div id="company">
      <h2 class="name">Origin, Enquêtes technico-légales inc.</h2>
      <div>1445, rue Notre-Dame, L’Ancienne-Lorette (Québec) G2E 3A8</div>
      <div>Tel.: 418 614-3344 | info@origin.expert
      </div>
    </div>
  </header>
  {{#each data.widgets}}
  <div>
     <h3> {{ this.title }} </h3>
    <div class="flex-container">
      {{#each this.formattedData}}
      <div style="display: flex;"><strong>{{this.name}}: </strong> {{this.purentage}} ({{this.count}}) </div>
      {{/each}}
    </div> 

    {{#if this.widget}}
    <div >
    <img src="{{this.widget}}"/>
    </div>
    {{/if}}
  </div>
  {{/each}}
</body>

</html>